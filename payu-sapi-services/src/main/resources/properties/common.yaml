http:
  private:
    host: "${app.listener.host}"
    port: "${app.listener.port}"

app:
  name: "${project.artifactId}"
  version: "${project.version}"
  ramlVersion: "${ramlVersion}"

jsonLogger:
  disabledFields: ""
  contentFieldsDataMasking: ""
  
org:
  id: "${etb.orgId}"
  
payu:
  payment_path: "payments-api/4.0/service.cgi"
  reports_path: "reports-api/4.0/service.cgi"
