# ==============================================================================
#  LOCAL ENVIRONMENT PROPERTIES
#  Used for local development. Start with: -Denv=local
# ==============================================================================
  
# --- API Manager ---
api:
  id: "0"
    
# --- Example Backend HTTP Requester ---
directv:
  protocol: "HTTPS"
  host: "api-gateway-qam.dtvpan.com"
  port: "443"
  sendercountry: "CO"
  basePath: ""
  token:
    path: "/token"
    post:
      method: "POST"
  estadoCliente:
    path: "/api/partnerView/1.0/{id}"
    get:
      method: "GET"
  altaCustomer:
    path: "/CustomerManagement/17.0/customerAndProducts"
    get:
      method: "GET"
    post:
      method: "POST"