# ==============================================================================
#  DEV ENVIRONMENT PROPERTIES
#  Used for development environment. Start with: -Denv=development
# ==============================================================================

# --- API Manager ---
api:
  id: "0"
    
# --- Zsmart HTTP Requester ---
zsmart:
  protocol: "HTTP"
  host: "47.85.185.254"
  port: "80"
  basePath: "/api"
  billing:
    path: "/rest"
    post:
      method: "POST"

# --- Zsmart Token HTTP Requester ---
zsmartToken:
  protocol: "HTTP"
  host: "47.85.185.254"
  port: "80"
  basePath: "/api"
  auth:
    responseType: "token"
    path: "/oauth2/apigAuthorize"
    post:
      method: "POST"
    userProfile: 
      appCode: "USER_PROFILE"
    subscriberProfile: 
      appCode: "SUBS_PROFILE"
    balanceMgmtProfile:
      appCode: "BALANCE_MANAGE"
    accountReceivableProfile:
      appCode: "ACCOUNT_RECE"
    offerSyncProfile:
      appCode: "OFFER_SYNC"