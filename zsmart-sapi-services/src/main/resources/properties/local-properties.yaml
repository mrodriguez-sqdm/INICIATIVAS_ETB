# ==============================================================================
#  LOCAL ENVIRONMENT PROPERTIES
#  Used for local development. Start with: -Denv=local
# ==============================================================================
  
# --- API Manager ---
api:
  id: "0"
    
# --- Zsmart HTTP Requester ---
zsmart:
  protocol: "HTTP"
  host: "43.111.228.223"
  port: "80"
  basePath: "/api"
  auth:
    responseType: "token"
    path: "/oauth2/apigAuthorize"
    post:
      method: "POST"
    userProfile: 
      appCode: "91492"    
  billing:
    path: "/rest"
    post:
      method: "POST"