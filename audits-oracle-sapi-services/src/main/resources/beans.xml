<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"

	xsi:schemaLocation="http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
      http://www.springframework.org/schema/jdbc
      http://www.springframework.org/schema/jdbc/spring-jdbc-4.2.xsd
      http://www.springframework.org/schema/context
      http://www.springframework.org/schema/context/spring-context-4.2.xsd
      http://www.springframework.org/schema/security
      http://www.springframework.org/schema/security/spring-security-4.2.xsd">

	<bean id="oracleDataSource"
		class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
		<property name="driverClassName"
			value="oracle.jdbc.driver.OracleDriver" />
		<property name="jdbcUrl"
			value="jdbc:oracle:thin:@${oracleDB.host}:${oracleDB.port}/${oracleDB.sid}" />
		<property name="username" value="${oracleDB.username}"/>
		<property name="password" value="${oracleDB.password}" />
		<property name="poolName" value="oracleConnectionPool" />
		<property name="maximumPoolSize" value="${oracleDB.poolSize}" />
		<property name="connectionTimeout" value="${oracleDB.connectionTimeout}" />
		
		<property name="minimumIdle" value="${oracleDB.minimumIdle}" />
		<property name="idleTimeout" value="${oracleDB.idleTimeout}" />
		<property name="maxLifetime" value="${oracleDB.maxLifetime}" />
		<property name="validationTimeout" value="${oracleDB.validationTimeout}" />
		<property name="connectionTestQuery" value="${oracleDB.connectionTestQuery}" />
	</bean>

</beans>