{
"transaction": {
"id": "string (UUID) — M — correlationId",
"timestamp": "datetime ISO-8601 — M"
},
"customer": {
"externalId": "string — M — custId en ZSmart",
"name": "string — M",
"type": "enum: PERSONAL | BUSINESS — M",
"documentType": "enum: CC | NIT | PASSPORT — M",
"documentNumber": "string — M",
"birthDate": "string YYYYMMDD — O",
"isVip": "boolean — M",
"status": "enum: ACTIVE | INACTIVE — M"
},
"account": {
"externalId": "string — M — acctId en ZSmart",
"paymentMode": "enum: POSTPAID | PREPAID — M",
"creditLimit": "number — O",
"billingCycleId": "string default '-1' — M"
},
"service": {
"externalId": "string — M — prodInstId en ZSmart",
"productCode": "string — M — PSTN | DSL | GPON | IPTV...",
"areaCode": "string — M — ej: 601",
"serviceNumber": "string — M — sin prefijo",
"effectiveDate": "string YYYYMMDDHHMMSS — M",
"offerCode": "string — M — código plan principal",
"offerInstanceId": "string — M — ID instancia oferta CRM",
"charges": [{
"type": "enum: ONE_OFF | DEPOSIT | ADVANCE — O",
"amount": "number — O",
"itemTypeCode": "string — M si hay cargos"
}]
}
}