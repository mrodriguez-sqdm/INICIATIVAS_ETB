# ==============================================================================
#  QA ENVIRONMENT PROPERTIES
#  Used for QA/staging environment. Start with: -Denv=qa
# ==============================================================================

# --- API Manager ---
api:
  id: "0"
    
# --- Pau HTTP Requester ---
pau:
  protocol: "HTTPS"
  host: "api-etb.pau.zone"
  port: ""
  basePath: "/api10"
  login:
    path: "/login"
    post:
      method: "POST"
  connectorById:
    path: "/conector-by-identificador"
    post:
      method: "POST"
  notification:
    path: "/notificacion-set"
    post:
      method: "POST"