# ==============================================================================
#  PROD ENVIRONMENT PROPERTIES
#  Used for production environment. Start with: -Denv=prod
# ==============================================================================

# --- API Manager ---
api:
  id: "20593061"

# --- Common PAPI HTTP Requester ---
commonPapi:
  protocol: "HTTP"
  host: "mule-worker-internal-common-papi-services-v2-prod.us-e2.cloudhub.io"
  port: "8091"
  basePath: "/v2"
  parameters:
    path: "/parameters"
    get:
      method: "GET"
  commMessage:
    path: "/communications/message"
    post:
      method: "POST"