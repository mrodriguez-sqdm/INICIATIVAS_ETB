# ==============================================================================
#  DEV ENVIRONMENT PROPERTIES
#  Used for development environment. Start with: -Denv=development
# ==============================================================================

# --- API Manager ---
api:
  id: "0"
    
# --- Example Backend HTTP Requester ---
order-mngt-papi:
  protocol: "HTTP"
  host: "mule-worker-internal-order-management-papi-services-dev.us-e2.cloudhub.io"
  port: "8091"
  basePath: "/v1"
  cancelProductOrder:
    path: "/cancelProductOrder"
    post:
      method: "POST"
  replaceSim:
    path: "/replaceSim"
    post:
      method: "POST"

tls:
  type: "JKS"
  path: "security/keystore.jks"
  keyPassword: "password"
  password: "password"