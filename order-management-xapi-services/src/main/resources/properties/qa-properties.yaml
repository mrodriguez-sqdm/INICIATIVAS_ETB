# ==============================================================================
#  QA ENVIRONMENT PROPERTIES
#  Used for QA/staging environment. Start with: -Denv=qa
# ==============================================================================

# --- API Manager ---
api:
  id: "0"
    
# --- Example Backend HTTP Requester ---
order-mngt-papi:
  protocol: "HTTP"
  host: "mule-worker-internal-order-management-papi-services-qa.us-e2.cloudhub.io"
  port: "8091"
  basePath: "/v1"
  cancelProductOrder:
    path: "/cancelProductOrder"
    post:
      method: "POST"
  replaceSim:
    path: "/replaceSim"
    post:
      method: "POST"

tls:
  type: "JKS"
  path: "security/keystore.jks"
  keyPassword: "password"
  password: "password"