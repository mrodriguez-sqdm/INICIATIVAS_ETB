# ==============================================================================
#  LOCAL ENVIRONMENT PROPERTIES
#  Used for local development. Start with: -Denv=local
# ==============================================================================
  
# --- API Manager ---
api:
  id: "0"
    
# --- Example Backend HTTP Requester ---
backend:
  protocol: "HTTPS"
  host: "dev-backend.example.com"
  port: "443"
  basePath: "/api/v1"
  resources:
    path: "/resources"
    get:
      method: "GET"
    post:
      method: "POST"